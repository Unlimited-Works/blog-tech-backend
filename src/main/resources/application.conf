blogtech {
  git {
    path = "~/.git-dir"
  }

  db {
    user = "postgres"
    password = ""
  }

  ssh {
    host = "localhost"
    user = ""
    password = ""
  }

  jwt {
    secert = "secert"
  }
}